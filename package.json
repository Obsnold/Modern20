{
  "name": "modern20",
  "private": true,
  "type": "module",
  "description": "Tooling for the Modern20 Foundry system. The system itself ships as plain ES modules and needs no build step; this manifest exists so the checks, the linters and the type checker are one command rather than nine.",
  "scripts": {
    "lint": "npm run lint:js && npm run lint:py",
    "lint:js": "eslint module scripts",
    "lint:py": "ruff check scripts",
    "typecheck": "tsc --noEmit",
    "check": "npm run check:python && npm run check:node",
    "check:python": "python3 scripts/check_globals.py && python3 scripts/check_app_props.py && python3 scripts/check_lang.py && python3 scripts/check_config.py && python3 scripts/check_shadowing.py && python3 scripts/check_packs.py && python3 scripts/check_coverage.py",
    "check:node": "node scripts/check_models.mjs && node scripts/check_templates.mjs && node scripts/check_casting.mjs && node scripts/check_combat.mjs && node scripts/check_creatures.mjs && node scripts/check_objects.mjs && node scripts/check_rules.mjs",
    "test": "npm run lint && npm run check",
    "extract": "python3 scripts/capture_edits.py",
    "extract:dry": "python3 scripts/capture_edits.py --dry-run",
    "deploy": "scripts/deploy.sh",
    "deploy:packs": "scripts/deploy.sh --packs"
  },
  "devDependencies": {
    "@astral-sh/ruff": ">=0.6",
    "eslint": "^9.0.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=20"
  }
}
